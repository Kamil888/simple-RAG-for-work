# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a simple Retrieval-Augmented Generation (RAG) system intended for work use cases. The project is in early/setup stage — update this file as the architecture takes shape.

## Architecture Notes

> **TODO:** Populate this section once source files are added. Key things to document:
> - Ingestion pipeline (how documents are chunked and embedded)
> - Vector store choice and configuration
> - Retrieval strategy (similarity search, hybrid, reranking)
> - LLM integration and prompt structure
> - Any API layer (FastAPI, Gradio, CLI, etc.)

## Common Commands

> **TODO:** Add build, run, and test commands once the stack is defined.

## Configuration

> **TODO:** Document required environment variables (e.g., API keys, vector DB connection) once `.env.example` is created.
